import {StyleSheet} from "react-native";

export const stylesUtils = StyleSheet.create({
    loading: {
        position: 'absolute',
        left: 0,
        right: 0,
        top: 20,
        bottom: 0,
        alignItems: 'center',
        justifyContent: 'flex-start'
    }
});